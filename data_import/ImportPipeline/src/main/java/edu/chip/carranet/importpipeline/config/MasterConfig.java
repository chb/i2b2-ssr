package edu.chip.carranet.importpipeline.config;


/**
 * The OneConfig, one config to rule them all.  This config should read all the config
 * for this application, but internal to this application the configs are split by
 * function.  To differentiate, each internal config has a prefix to its config to
 * separate itself from other functions.  They take the form:
 *
 * prefix.key = value
 *
 * 
 *
 * @author Justin Quan
 * Date: Oct 19, 2010
 */
public class MasterConfig {
    
}
